import torch

def generate_greedy(
        input_ids,
        model,
        max_new_tokens,
        eos_token_id
) -> torch.Tensor:
    
    raise NotImplementedError(
        "Not yet implemented"
    )